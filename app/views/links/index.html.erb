<h1>Homepage</h1>

<ul>
  <% @links.each do |link| %>
    <li><%= link_to link.description, link.url %>  <button class="btn btn-default btn-mini"><%= link_to "Delete", link, :method => :delete, :data => { :confirm  => "are you sure about that?" } %></button><hr/></li>
  <% end %>
</ul>

<p><%= link_to "New link", new_link_path %></p>
